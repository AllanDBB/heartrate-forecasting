# Core — solo dependencias que NO rompen Colab.
# Las dependencias específicas de cada modelo se instalan en su celda del notebook.
scikit-learn
joblib
openpyxl
matplotlib
pyyaml

# PyTorch (ya viene en Colab, pero por si acaso)
torch
torchvision
transformers
accelerate
huggingface_hub

# Las siguientes se instalan en su celda respectiva del notebook:
#   MOMENT:  pip install git+https://github.com/moment-timeseries-foundation-model/moment.git
#   Moirai:  pip install uni2ts gluonts einops
#   TCN:     pip install keras-tcn tensorflow